- [x] Explain why 404 happens with current Vercel root (`spoken-engliish-main/`).
- [x] Change Vercel Project Root directory to `stitch_ai_spoken_english_tutor/` (must be done in Vercel UI).
- [ ] Redeploy.
- [ ] Verify endpoints: `/api/health` and `/`.
- [ ] If `/api/health` still 404, verify Vercel is picking up the correct `vercel.json` and try a simpler test deploy.


